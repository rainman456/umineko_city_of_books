// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: ship.sql

package sqlcgen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countShips = `-- name: CountShips :one
SELECT COUNT(*) FROM ships s
WHERE ($1::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.series = $1::text))
  AND ($2::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.character_id = $2::text))
  AND (NOT $3::boolean OR COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) <= $4::int)
  AND ($5::text = '' OR s.user_id <> ALL(string_to_array($5::text, ',')::uuid[]))
`

type CountShipsParams struct {
	Column1 string
	Column2 string
	Column3 bool
	Column4 int32
	Column5 string
}

func (q *Queries) CountShips(ctx context.Context, arg CountShipsParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countShips,
		arg.Column1,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countShipsByUser = `-- name: CountShipsByUser :one
SELECT COUNT(*) FROM ships WHERE user_id = $1
`

func (q *Queries) CountShipsByUser(ctx context.Context, userID uuid.UUID) (int64, error) {
	row := q.db.QueryRowContext(ctx, countShipsByUser, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createShip = `-- name: CreateShip :one
WITH s AS (
    INSERT INTO ships (user_id, title, description, image_url, thumbnail_url)
    VALUES ($1, $2, $3, '', '')
    RETURNING id, user_id, title, description, image_url, thumbnail_url, created_at, updated_at
)
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       0::bigint AS vote_score,
       0::int AS user_vote,
       0::bigint AS comment_count
FROM s
JOIN users u ON u.id = s.user_id
LEFT JOIN user_roles r ON r.user_id = s.user_id
`

type CreateShipParams struct {
	UserID      uuid.UUID
	Title       string
	Description string
}

type CreateShipRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) CreateShip(ctx context.Context, arg CreateShipParams) (CreateShipRow, error) {
	row := q.db.QueryRowContext(ctx, createShip, arg.UserID, arg.Title, arg.Description)
	var i CreateShipRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Title,
		&i.Description,
		&i.ImageUrl,
		&i.ThumbnailUrl,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Username,
		&i.DisplayName,
		&i.AvatarUrl,
		&i.AuthorRole,
		&i.VoteScore,
		&i.UserVote,
		&i.CommentCount,
	)
	return i, err
}

const deleteShipCharacters = `-- name: DeleteShipCharacters :exec
DELETE FROM ship_characters WHERE ship_id = $1
`

func (q *Queries) DeleteShipCharacters(ctx context.Context, shipID uuid.UUID) error {
	_, err := q.db.ExecContext(ctx, deleteShipCharacters, shipID)
	return err
}

const getShipByID = `-- name: GetShipByID :one
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE s.id = $2
`

type GetShipByIDParams struct {
	UserID uuid.UUID
	ID     uuid.UUID
}

type GetShipByIDRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) GetShipByID(ctx context.Context, arg GetShipByIDParams) (GetShipByIDRow, error) {
	row := q.db.QueryRowContext(ctx, getShipByID, arg.UserID, arg.ID)
	var i GetShipByIDRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Title,
		&i.Description,
		&i.ImageUrl,
		&i.ThumbnailUrl,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Username,
		&i.DisplayName,
		&i.AvatarUrl,
		&i.AuthorRole,
		&i.VoteScore,
		&i.UserVote,
		&i.CommentCount,
	)
	return i, err
}

const getShipCharacters = `-- name: GetShipCharacters :many
SELECT id, ship_id, series, character_id, character_name, sort_order
FROM ship_characters
WHERE ship_id = $1
ORDER BY sort_order ASC
`

func (q *Queries) GetShipCharacters(ctx context.Context, shipID uuid.UUID) ([]ShipCharacter, error) {
	rows, err := q.db.QueryContext(ctx, getShipCharacters, shipID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ShipCharacter{}
	for rows.Next() {
		var i ShipCharacter
		if err := rows.Scan(
			&i.ID,
			&i.ShipID,
			&i.Series,
			&i.CharacterID,
			&i.CharacterName,
			&i.SortOrder,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getShipCharactersBatch = `-- name: GetShipCharactersBatch :many
SELECT id, ship_id, series, character_id, character_name, sort_order
FROM ship_characters
WHERE ship_id = ANY(string_to_array($1::text, ',')::uuid[])
ORDER BY sort_order ASC
`

func (q *Queries) GetShipCharactersBatch(ctx context.Context, dollar_1 string) ([]ShipCharacter, error) {
	rows, err := q.db.QueryContext(ctx, getShipCharactersBatch, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ShipCharacter{}
	for rows.Next() {
		var i ShipCharacter
		if err := rows.Scan(
			&i.ID,
			&i.ShipID,
			&i.Series,
			&i.CharacterID,
			&i.CharacterName,
			&i.SortOrder,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getShipImagePaths = `-- name: GetShipImagePaths :one
SELECT image_url, thumbnail_url FROM ships WHERE id = $1
`

type GetShipImagePathsRow struct {
	ImageUrl     string
	ThumbnailUrl string
}

func (q *Queries) GetShipImagePaths(ctx context.Context, id uuid.UUID) (GetShipImagePathsRow, error) {
	row := q.db.QueryRowContext(ctx, getShipImagePaths, id)
	var i GetShipImagePathsRow
	err := row.Scan(&i.ImageUrl, &i.ThumbnailUrl)
	return i, err
}

const insertShipCharacter = `-- name: InsertShipCharacter :exec
INSERT INTO ship_characters (ship_id, series, character_id, character_name, sort_order)
VALUES ($1, $2, $3, $4, $5)
`

type InsertShipCharacterParams struct {
	ShipID        uuid.UUID
	Series        string
	CharacterID   string
	CharacterName string
	SortOrder     int32
}

func (q *Queries) InsertShipCharacter(ctx context.Context, arg InsertShipCharacterParams) error {
	_, err := q.db.ExecContext(ctx, insertShipCharacter,
		arg.ShipID,
		arg.Series,
		arg.CharacterID,
		arg.CharacterName,
		arg.SortOrder,
	)
	return err
}

const listShipsByComments = `-- name: ListShipsByComments :many
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE ($2::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.series = $2::text))
  AND ($3::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.character_id = $3::text))
  AND (NOT $4::boolean OR COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) <= $5::int)
  AND ($6::text = '' OR s.user_id <> ALL(string_to_array($6::text, ',')::uuid[]))
ORDER BY (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id) DESC, s.created_at DESC
LIMIT $7 OFFSET $8
`

type ListShipsByCommentsParams struct {
	UserID  uuid.UUID
	Column2 string
	Column3 string
	Column4 bool
	Column5 int32
	Column6 string
	Limit   int32
	Offset  int32
}

type ListShipsByCommentsRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) ListShipsByComments(ctx context.Context, arg ListShipsByCommentsParams) ([]ListShipsByCommentsRow, error) {
	rows, err := q.db.QueryContext(ctx, listShipsByComments,
		arg.UserID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
		arg.Column6,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListShipsByCommentsRow{}
	for rows.Next() {
		var i ListShipsByCommentsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.ImageUrl,
			&i.ThumbnailUrl,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Username,
			&i.DisplayName,
			&i.AvatarUrl,
			&i.AuthorRole,
			&i.VoteScore,
			&i.UserVote,
			&i.CommentCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listShipsByControversial = `-- name: ListShipsByControversial :many
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE ($2::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.series = $2::text))
  AND ($3::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.character_id = $3::text))
  AND (NOT $4::boolean OR COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) <= $5::int)
  AND ($6::text = '' OR s.user_id <> ALL(string_to_array($6::text, ',')::uuid[]))
ORDER BY (
    (SELECT COUNT(*) FROM ship_votes v WHERE v.ship_id = s.id AND v.value = 1) *
    (SELECT COUNT(*) FROM ship_votes v WHERE v.ship_id = s.id AND v.value = -1)
) DESC, s.created_at DESC
LIMIT $7 OFFSET $8
`

type ListShipsByControversialParams struct {
	UserID  uuid.UUID
	Column2 string
	Column3 string
	Column4 bool
	Column5 int32
	Column6 string
	Limit   int32
	Offset  int32
}

type ListShipsByControversialRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) ListShipsByControversial(ctx context.Context, arg ListShipsByControversialParams) ([]ListShipsByControversialRow, error) {
	rows, err := q.db.QueryContext(ctx, listShipsByControversial,
		arg.UserID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
		arg.Column6,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListShipsByControversialRow{}
	for rows.Next() {
		var i ListShipsByControversialRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.ImageUrl,
			&i.ThumbnailUrl,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Username,
			&i.DisplayName,
			&i.AvatarUrl,
			&i.AuthorRole,
			&i.VoteScore,
			&i.UserVote,
			&i.CommentCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listShipsByCrackship = `-- name: ListShipsByCrackship :many
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE ($2::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.series = $2::text))
  AND ($3::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.character_id = $3::text))
  AND (NOT $4::boolean OR COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) <= $5::int)
  AND ($6::text = '' OR s.user_id <> ALL(string_to_array($6::text, ',')::uuid[]))
ORDER BY COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) ASC, s.created_at DESC
LIMIT $7 OFFSET $8
`

type ListShipsByCrackshipParams struct {
	UserID  uuid.UUID
	Column2 string
	Column3 string
	Column4 bool
	Column5 int32
	Column6 string
	Limit   int32
	Offset  int32
}

type ListShipsByCrackshipRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) ListShipsByCrackship(ctx context.Context, arg ListShipsByCrackshipParams) ([]ListShipsByCrackshipRow, error) {
	rows, err := q.db.QueryContext(ctx, listShipsByCrackship,
		arg.UserID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
		arg.Column6,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListShipsByCrackshipRow{}
	for rows.Next() {
		var i ListShipsByCrackshipRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.ImageUrl,
			&i.ThumbnailUrl,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Username,
			&i.DisplayName,
			&i.AvatarUrl,
			&i.AuthorRole,
			&i.VoteScore,
			&i.UserVote,
			&i.CommentCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listShipsByNew = `-- name: ListShipsByNew :many
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE ($2::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.series = $2::text))
  AND ($3::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.character_id = $3::text))
  AND (NOT $4::boolean OR COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) <= $5::int)
  AND ($6::text = '' OR s.user_id <> ALL(string_to_array($6::text, ',')::uuid[]))
ORDER BY s.created_at DESC
LIMIT $7 OFFSET $8
`

type ListShipsByNewParams struct {
	UserID  uuid.UUID
	Column2 string
	Column3 string
	Column4 bool
	Column5 int32
	Column6 string
	Limit   int32
	Offset  int32
}

type ListShipsByNewRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) ListShipsByNew(ctx context.Context, arg ListShipsByNewParams) ([]ListShipsByNewRow, error) {
	rows, err := q.db.QueryContext(ctx, listShipsByNew,
		arg.UserID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
		arg.Column6,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListShipsByNewRow{}
	for rows.Next() {
		var i ListShipsByNewRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.ImageUrl,
			&i.ThumbnailUrl,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Username,
			&i.DisplayName,
			&i.AvatarUrl,
			&i.AuthorRole,
			&i.VoteScore,
			&i.UserVote,
			&i.CommentCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listShipsByOld = `-- name: ListShipsByOld :many
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE ($2::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.series = $2::text))
  AND ($3::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.character_id = $3::text))
  AND (NOT $4::boolean OR COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) <= $5::int)
  AND ($6::text = '' OR s.user_id <> ALL(string_to_array($6::text, ',')::uuid[]))
ORDER BY s.created_at ASC
LIMIT $7 OFFSET $8
`

type ListShipsByOldParams struct {
	UserID  uuid.UUID
	Column2 string
	Column3 string
	Column4 bool
	Column5 int32
	Column6 string
	Limit   int32
	Offset  int32
}

type ListShipsByOldRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) ListShipsByOld(ctx context.Context, arg ListShipsByOldParams) ([]ListShipsByOldRow, error) {
	rows, err := q.db.QueryContext(ctx, listShipsByOld,
		arg.UserID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
		arg.Column6,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListShipsByOldRow{}
	for rows.Next() {
		var i ListShipsByOldRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.ImageUrl,
			&i.ThumbnailUrl,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Username,
			&i.DisplayName,
			&i.AvatarUrl,
			&i.AuthorRole,
			&i.VoteScore,
			&i.UserVote,
			&i.CommentCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listShipsByTop = `-- name: ListShipsByTop :many
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE ($2::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.series = $2::text))
  AND ($3::text = '' OR EXISTS(SELECT 1 FROM ship_characters sc WHERE sc.ship_id = s.id AND sc.character_id = $3::text))
  AND (NOT $4::boolean OR COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) <= $5::int)
  AND ($6::text = '' OR s.user_id <> ALL(string_to_array($6::text, ',')::uuid[]))
ORDER BY COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0) DESC, s.created_at DESC
LIMIT $7 OFFSET $8
`

type ListShipsByTopParams struct {
	UserID  uuid.UUID
	Column2 string
	Column3 string
	Column4 bool
	Column5 int32
	Column6 string
	Limit   int32
	Offset  int32
}

type ListShipsByTopRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) ListShipsByTop(ctx context.Context, arg ListShipsByTopParams) ([]ListShipsByTopRow, error) {
	rows, err := q.db.QueryContext(ctx, listShipsByTop,
		arg.UserID,
		arg.Column2,
		arg.Column3,
		arg.Column4,
		arg.Column5,
		arg.Column6,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListShipsByTopRow{}
	for rows.Next() {
		var i ListShipsByTopRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.ImageUrl,
			&i.ThumbnailUrl,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Username,
			&i.DisplayName,
			&i.AvatarUrl,
			&i.AuthorRole,
			&i.VoteScore,
			&i.UserVote,
			&i.CommentCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listShipsByUser = `-- name: ListShipsByUser :many
SELECT s.id, s.user_id, s.title, s.description, s.image_url, s.thumbnail_url, s.created_at, s.updated_at,
       u.username, u.display_name, u.avatar_url,
       COALESCE(r.role, '')::text AS author_role,
       COALESCE((SELECT SUM(v.value) FROM ship_votes v WHERE v.ship_id = s.id), 0)::bigint AS vote_score,
       COALESCE((SELECT v.value FROM ship_votes v WHERE v.ship_id = s.id AND v.user_id = $1), 0)::int AS user_vote,
       (SELECT COUNT(*) FROM ship_comments c WHERE c.ship_id = s.id)::bigint AS comment_count
FROM ships s
JOIN users u ON s.user_id = u.id
LEFT JOIN user_roles r ON r.user_id = s.user_id
WHERE s.user_id = $2
ORDER BY s.created_at DESC
LIMIT $3 OFFSET $4
`

type ListShipsByUserParams struct {
	UserID   uuid.UUID
	UserID_2 uuid.UUID
	Limit    int32
	Offset   int32
}

type ListShipsByUserRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	Title        string
	Description  string
	ImageUrl     string
	ThumbnailUrl string
	CreatedAt    time.Time
	UpdatedAt    time.Time
	Username     string
	DisplayName  string
	AvatarUrl    string
	AuthorRole   string
	VoteScore    int64
	UserVote     int32
	CommentCount int64
}

func (q *Queries) ListShipsByUser(ctx context.Context, arg ListShipsByUserParams) ([]ListShipsByUserRow, error) {
	rows, err := q.db.QueryContext(ctx, listShipsByUser,
		arg.UserID,
		arg.UserID_2,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListShipsByUserRow{}
	for rows.Next() {
		var i ListShipsByUserRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.ImageUrl,
			&i.ThumbnailUrl,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Username,
			&i.DisplayName,
			&i.AvatarUrl,
			&i.AuthorRole,
			&i.VoteScore,
			&i.UserVote,
			&i.CommentCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateShipDetails = `-- name: UpdateShipDetails :execrows
UPDATE ships SET title = $1, description = $2, updated_at = NOW() WHERE id = $3 AND user_id = $4
`

type UpdateShipDetailsParams struct {
	Title       string
	Description string
	ID          uuid.UUID
	UserID      uuid.UUID
}

func (q *Queries) UpdateShipDetails(ctx context.Context, arg UpdateShipDetailsParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, updateShipDetails,
		arg.Title,
		arg.Description,
		arg.ID,
		arg.UserID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const updateShipDetailsAsAdmin = `-- name: UpdateShipDetailsAsAdmin :execrows
UPDATE ships SET title = $1, description = $2, updated_at = NOW() WHERE id = $3
`

type UpdateShipDetailsAsAdminParams struct {
	Title       string
	Description string
	ID          uuid.UUID
}

func (q *Queries) UpdateShipDetailsAsAdmin(ctx context.Context, arg UpdateShipDetailsAsAdminParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, updateShipDetailsAsAdmin, arg.Title, arg.Description, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const updateShipImage = `-- name: UpdateShipImage :exec
UPDATE ships SET image_url = $1, thumbnail_url = $2 WHERE id = $3
`

type UpdateShipImageParams struct {
	ImageUrl     string
	ThumbnailUrl string
	ID           uuid.UUID
}

func (q *Queries) UpdateShipImage(ctx context.Context, arg UpdateShipImageParams) error {
	_, err := q.db.ExecContext(ctx, updateShipImage, arg.ImageUrl, arg.ThumbnailUrl, arg.ID)
	return err
}
