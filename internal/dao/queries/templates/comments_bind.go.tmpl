// Code generated by internal/dao/queries/expand; DO NOT EDIT.
// template: internal/dao/queries/templates/comments_bind.go.tmpl
// entity: {{.Entity}}

package dao

import (
	"context"
	"database/sql"

	"github.com/google/uuid"

	"umineko_city_of_books/internal/dao/sqlcgen"
	"umineko_city_of_books/internal/model"
	"umineko_city_of_books/internal/model/spec"
)

type (
	{{.Recv}} struct{}
)

func ({{.Recv}}) Create(ctx context.Context, q *sqlcgen.Queries, s spec.NewComment[{{.KeyType}}]) (*model.CommentRow, error) {
	row, err := q.Create{{.Entity}}Comment(ctx, sqlcgen.Create{{.Entity}}CommentParams{
		{{.FKField}}: s.TargetID,
		ParentID:     s.ParentID,
		UserID:       s.UserID,
		Body:         s.Body,
	})
	if err != nil {
		return nil, err
	}

	return new(commentRowFromCreate(commentCreateRow(row))), nil
}

func ({{.Recv}}) Update(ctx context.Context, q *sqlcgen.Queries, s spec.CommentUpdate) (int64, error) {
	return q.Update{{.Entity}}Comment(ctx, sqlcgen.Update{{.Entity}}CommentParams{
		Body:    s.Body,
		ID:      s.CommentID,
		Column3: s.AsAdmin,
		UserID:  s.UserID,
	})
}

func ({{.Recv}}) Delete(ctx context.Context, q *sqlcgen.Queries, s spec.CommentDeletion) (int64, error) {
	return q.Delete{{.Entity}}Comment(ctx, sqlcgen.Delete{{.Entity}}CommentParams{
		ID:      s.CommentID,
		Column2: s.AsAdmin,
		UserID:  s.UserID,
	})
}

func ({{.Recv}}) Count(ctx context.Context, q *sqlcgen.Queries, target {{.KeyType}}, exclude []uuid.UUID) (int64, error) {
	return q.Count{{.Entity}}Comments(ctx, sqlcgen.Count{{.Entity}}CommentsParams{
		{{.FKField}}: target,
		Column2:      joinUUIDs(exclude),
	})
}

func ({{.Recv}}) List(ctx context.Context, q *sqlcgen.Queries, s spec.CommentQuery[{{.KeyType}}]) ([]model.CommentRow, error) {
	rows, err := q.List{{.Entity}}Comments(ctx, sqlcgen.List{{.Entity}}CommentsParams{
		UserID:       s.ViewerID,
		{{.FKField}}: s.TargetID,
		Column3:      joinUUIDs(s.ExcludeUserIDs),
		Limit:        int32(s.Limit),
		Offset:       int32(s.Offset),
	})
	if err != nil {
		return nil, err
	}

	out := make([]model.CommentRow, 0, len(rows))
	for _, row := range rows {
		out = append(out, commentRowFromList(commentListRow(row)))
	}

	return out, nil
}

func ({{.Recv}}) ByID(ctx context.Context, q *sqlcgen.Queries, id uuid.UUID) (*model.CommentRow, error) {
	row, err := q.Get{{.Entity}}CommentByID(ctx, id)
	if err != nil {
		return nil, err
	}

	return new(commentRowFromCreate(commentCreateRow(row))), nil
}

func ({{.Recv}}) EntityID(ctx context.Context, q *sqlcgen.Queries, id uuid.UUID) ({{.KeyType}}, error) {
	return q.Get{{.Entity}}CommentEntityID(ctx, id)
}

func ({{.Recv}}) AuthorID(ctx context.Context, q *sqlcgen.Queries, id uuid.UUID) (uuid.UUID, error) {
	return q.Get{{.Entity}}CommentAuthorID(ctx, id)
}

func ({{.Recv}}) Like(ctx context.Context, q *sqlcgen.Queries, s spec.CommentLike) error {
	return q.Like{{.Entity}}Comment(ctx, sqlcgen.Like{{.Entity}}CommentParams{UserID: s.UserID, CommentID: s.CommentID})
}

func ({{.Recv}}) Unlike(ctx context.Context, q *sqlcgen.Queries, s spec.CommentLike) error {
	return q.Unlike{{.Entity}}Comment(ctx, sqlcgen.Unlike{{.Entity}}CommentParams{UserID: s.UserID, CommentID: s.CommentID})
}

func ({{.Recv}}) AddMedia(ctx context.Context, q *sqlcgen.Queries, s spec.NewMedia) (int64, error) {
	return q.Add{{.Entity}}CommentMedia(ctx, sqlcgen.Add{{.Entity}}CommentMediaParams{
		CommentID:    s.TargetID,
		MediaUrl:     s.MediaURL,
		MediaType:    s.MediaType,
		ThumbnailUrl: s.ThumbnailURL,
		Filename:     sql.NullString{String: s.Filename, Valid: true},
		Column6:      int32(s.SortOrder),
		IsSpoiler:    s.IsSpoiler,
	})
}

func ({{.Recv}}) Media(ctx context.Context, q *sqlcgen.Queries, commentID uuid.UUID) ([]model.PostMediaRow, error) {
	rows, err := q.Get{{.Entity}}CommentMedia(ctx, commentID)
	if err != nil {
		return nil, err
	}

	out := make([]model.PostMediaRow, 0, len(rows))
	for _, row := range rows {
		out = append(out, mediaRowFromComment(commentMediaRow(row)))
	}

	return out, nil
}

func ({{.Recv}}) MediaBatch(ctx context.Context, q *sqlcgen.Queries, ids []uuid.UUID) (map[uuid.UUID][]model.PostMediaRow, error) {
	rows, err := q.Get{{.Entity}}CommentMediaBatch(ctx, joinUUIDs(ids))
	if err != nil {
		return nil, err
	}

	out := make(map[uuid.UUID][]model.PostMediaRow)
	for _, row := range rows {
		out[row.CommentID] = append(out[row.CommentID], mediaRowFromComment(commentMediaRow(row)))
	}

	return out, nil
}

func ({{.Recv}}) SetMediaURL(ctx context.Context, q *sqlcgen.Queries, s spec.MediaURLUpdate) error {
	return q.Update{{.Entity}}CommentMediaURL(ctx, sqlcgen.Update{{.Entity}}CommentMediaURLParams{MediaUrl: s.URL, ID: s.ID})
}

func ({{.Recv}}) SetMediaThumbnail(ctx context.Context, q *sqlcgen.Queries, s spec.MediaURLUpdate) error {
	return q.Update{{.Entity}}CommentMediaThumbnail(ctx, sqlcgen.Update{{.Entity}}CommentMediaThumbnailParams{ThumbnailUrl: s.URL, ID: s.ID})
}

func ({{.Recv}}) Paths(ctx context.Context, q *sqlcgen.Queries, target {{.KeyType}}) ([]string, error) {
	rows, err := q.Collect{{.Entity}}CommentMediaPaths(ctx, target)
	if err != nil {
		return nil, err
	}

	out := make([]mediaPathRow, 0, len(rows))
	for _, row := range rows {
		out = append(out, mediaPathRow(row))
	}

	return collectMediaPaths(out), nil
}

func ({{.Recv}}) SinglePaths(ctx context.Context, q *sqlcgen.Queries, commentID uuid.UUID) ([]string, error) {
	rows, err := q.CollectSingle{{.Entity}}CommentMediaPaths(ctx, commentID)
	if err != nil {
		return nil, err
	}

	out := make([]mediaPathRow, 0, len(rows))
	for _, row := range rows {
		out = append(out, mediaPathRow(row))
	}

	return collectMediaPaths(out), nil
}
