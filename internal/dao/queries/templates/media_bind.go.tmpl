// Code generated by internal/dao/queries/expand; DO NOT EDIT.
// template: internal/dao/queries/templates/media_bind.go.tmpl
// entity: {{.Entity}}

package dao

import (
	"context"
	"database/sql"

	"github.com/google/uuid"

	"umineko_city_of_books/internal/dao/sqlcgen"
	"umineko_city_of_books/internal/model"
	"umineko_city_of_books/internal/model/spec"
)

type (
	{{.Recv}} struct{}
)

func ({{.Recv}}) Add(ctx context.Context, q *sqlcgen.Queries, s spec.NewMedia) (int64, error) {
	return q.Add{{.Entity}}Media(ctx, sqlcgen.Add{{.Entity}}MediaParams{
		{{.FKField}}: s.TargetID,
		MediaUrl:     s.MediaURL,
		MediaType:    s.MediaType,
		ThumbnailUrl: s.ThumbnailURL,
		Filename:     sql.NullString{String: s.Filename, Valid: true},
		Column6:      int32(s.SortOrder),
		IsSpoiler:    s.IsSpoiler,
	})
}

func ({{.Recv}}) URL(ctx context.Context, q *sqlcgen.Queries, s spec.MediaDeletion) (string, error) {
	return q.Get{{.Entity}}MediaURL(ctx, sqlcgen.Get{{.Entity}}MediaURLParams{ID: s.ID, {{.FKField}}: s.TargetID})
}

func ({{.Recv}}) DeleteRow(ctx context.Context, q *sqlcgen.Queries, s spec.MediaDeletion) error {
	return q.Delete{{.Entity}}MediaRow(ctx, sqlcgen.Delete{{.Entity}}MediaRowParams{ID: s.ID, {{.FKField}}: s.TargetID})
}

func ({{.Recv}}) SetURL(ctx context.Context, q *sqlcgen.Queries, s spec.MediaURLUpdate) error {
	return q.Update{{.Entity}}MediaURL(ctx, sqlcgen.Update{{.Entity}}MediaURLParams{MediaUrl: s.URL, ID: s.ID})
}

func ({{.Recv}}) SetThumbnail(ctx context.Context, q *sqlcgen.Queries, s spec.MediaURLUpdate) error {
	return q.Update{{.Entity}}MediaThumbnail(ctx, sqlcgen.Update{{.Entity}}MediaThumbnailParams{ThumbnailUrl: s.URL, ID: s.ID})
}

func ({{.Recv}}) Media(ctx context.Context, q *sqlcgen.Queries, targetID uuid.UUID) ([]model.PostMediaRow, error) {
	rows, err := q.Get{{.Entity}}Media(ctx, targetID)
	if err != nil {
		return nil, err
	}

	out := make([]model.PostMediaRow, 0, len(rows))
	for _, row := range rows {
		out = append(out, mediaRowFromEntity(entityMediaRow(row)))
	}

	return out, nil
}

func ({{.Recv}}) MediaBatch(ctx context.Context, q *sqlcgen.Queries, targetIDs []uuid.UUID) (map[uuid.UUID][]model.PostMediaRow, error) {
	rows, err := q.Get{{.Entity}}MediaBatch(ctx, joinUUIDs(targetIDs))
	if err != nil {
		return nil, err
	}

	out := make(map[uuid.UUID][]model.PostMediaRow)
	for _, row := range rows {
		mapped := mediaRowFromEntity(entityMediaRow(row))
		out[mapped.PostID] = append(out[mapped.PostID], mapped)
	}

	return out, nil
}

func ({{.Recv}}) Paths(ctx context.Context, q *sqlcgen.Queries, targetID uuid.UUID) ([]string, error) {
	rows, err := q.Collect{{.Entity}}MediaPaths(ctx, targetID)
	if err != nil {
		return nil, err
	}

	out := make([]mediaPathRow, 0, len(rows))
	for _, row := range rows {
		out = append(out, mediaPathRow(row))
	}

	return collectMediaPaths(out), nil
}
