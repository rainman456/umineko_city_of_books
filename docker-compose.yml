x-logging: &default-logging
  driver: json-file
  options:
    max-size: "50m"
    max-file: "3"

services:
  postgres:
    image: postgres:latest
    container_name: umineko-postgres
    restart: unless-stopped
    logging: *default-logging
    env_file:
      - postgres.env
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - "127.0.0.1:5007:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  umineko-city-of-books:
    build: .
    container_name: umineko-city-of-books
    restart: unless-stopped
    logging: *default-logging
    stop_grace_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
      valkey-cache:
        condition: service_started
    ports:
      - "2312:4323"
    env_file:
      - .env
      - postgres.env
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./data:/app/data
      - ./fcm-service-account.json:/app/fcm-service-account.json:ro

  valkey:
    image: valkey/valkey:latest
    container_name: umineko-valkey
    restart: unless-stopped
    logging: *default-logging
    command: valkey-server --save "" --appendonly no
    ports:
      - "127.0.0.1:6379:6379"

  valkey-cache:
    image: valkey/valkey:latest
    container_name: umineko-valkey-cache
    restart: unless-stopped
    logging: *default-logging
    command: valkey-server --save "" --appendonly no --maxmemory 4gb --maxmemory-policy allkeys-lru
    ports:
      - "127.0.0.1:6381:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 5s

  livekit:
    image: livekit/livekit-server:latest
    container_name: umineko-livekit
    restart: unless-stopped
    logging: *default-logging
    command: --config /etc/livekit.yaml
    depends_on:
      - valkey
    environment:
      NODE_IP: 127.0.0.1
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882:7882/udp"
    volumes:
      - ./livekit.yaml:/etc/livekit.yaml:ro

  livekit-ingress:
    image: livekit/ingress:latest
    container_name: umineko-livekit-ingress
    restart: unless-stopped
    logging: *default-logging
    depends_on:
      - valkey
      - livekit
    environment:
      INGRESS_CONFIG_FILE: /etc/ingress.yaml
    ports:
      - "8080:8080"
      - "7885:7885/udp"
    volumes:
      - ./ingress.yaml:/etc/ingress.yaml:ro

  livekit-egress:
    image: livekit/egress:latest
    container_name: umineko-livekit-egress
    restart: unless-stopped
    logging: *default-logging
    cap_add:
      - SYS_ADMIN
    depends_on:
      - valkey
      - livekit
    environment:
      EGRESS_CONFIG_FILE: /etc/egress.yaml
    volumes:
      - ./egress.yaml:/etc/egress.yaml:ro
      - ./data/hls:/app/data/hls

volumes:
  postgres_data:
